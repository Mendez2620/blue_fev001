generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tbl_educative_offer {
  id           BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  level        String?   @db.VarChar(45)
  clave        String?   @db.VarChar(15)
  uniq         String?   @db.VarChar(15)
  municipality String?   @db.VarChar(45)
  logo         String?   @db.VarChar(45)
  areas        String?   @db.VarChar(45)
  name         String?   @db.VarChar(100)
  short_name   String?   @db.VarChar(30)
  description  String?   @db.VarChar(450)
  address      String?   @db.VarChar(250)
  telephone_1  String?   @db.VarChar(45)
  telephone_2  String?   @db.VarChar(45)
  email        String?   @db.VarChar(100)
  website      String?   @db.VarChar(250)
  facebook     String?   @db.VarChar(150)
  twitter      String?   @db.VarChar(150)
  instagram    String?   @db.VarChar(150)
  youtube      String?   @db.VarChar(150)
  linkedin     String?   @db.VarChar(150)
  redirect_url String?   @db.VarChar(500)
  whatsapp     String?   @db.VarChar(150)
  tiktok       String?   @db.VarChar(150)
  url_video    String?   @db.Text
  promotions   String?   @db.Text
  text_promo   String?   @db.Text
  pdf          String    @db.VarChar(250)
  created_at   DateTime? @db.DateTime(0)
  updated_at   DateTime? @db.DateTime(0)
  active       Int?      @default(0)
}

model tbl_educative_offer_areas {
  id         BigInt    @id @default(autoincrement())
  name       String?   @db.VarChar(50)
  created_at DateTime? @db.DateTime(0)
  updated_at DateTime? @db.DateTime(0)
  active     Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tbl_educative_offer_campus_careers {
  id                           BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  ev_educative_offer_campus_id BigInt?
  cv                           String?   @db.VarChar(200)
  name                         String?   @db.VarChar(200)
  modality                     String?   @db.VarChar(200)
  type_incorporation           String?   @db.VarChar(200)
  date_agreement               String?   @db.VarChar(200)
  num_agreement                String?   @db.VarChar(200)
  shift                        String?   @db.VarChar(200)
  created_at                   DateTime? @db.DateTime(0)
  updated_at                   DateTime? @db.DateTime(0)
  active                       Int?      @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tbl_educative_offer_campuses {
  id                    BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  ev_educative_offer_id String?   @db.VarChar(45)
  name                  String?   @db.VarChar(100)
  cv_cct                String?   @db.VarChar(20)
  level                 String?   @db.VarChar(45)
  municipality          String?   @db.VarChar(45)
  settlement            String?   @db.Text
  address               String?   @db.Text
  created_at            DateTime? @db.DateTime(0)
  updated_at            DateTime? @db.DateTime(0)
  active                Int?      @default(0)
}

model tbl_educative_offer_levels {
  id         BigInt    @id @default(autoincrement())
  name       String?   @db.VarChar(50)
  created_at DateTime? @db.DateTime(0)
  updated_at DateTime? @db.DateTime(0)
  active     Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model tbl_expo_practics {
  id                     BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  image                  String?   @default("default.png") @db.VarChar(45)
  name                   String?   @db.VarChar(200)
  slug                   String?   @db.VarChar(200)
  description            String?   @db.Text
  why                    String?   @db.Text
  level                  String?   @db.VarChar(150)
  institution            String?   @db.VarChar(150)
  institution_short_name String?   @db.VarChar(150)
  municipality           String?   @db.VarChar(150)
  career                 String?   @db.VarChar(150)
  video_embed            String?   @db.Text
  order                  Int?
  created_at             DateTime? @db.DateTime(0)
  updated_at             DateTime? @db.DateTime(0)
  active                 Int?      @default(1)
}

model User {
  id           String   @id @default(cuid()) @db.VarChar(191)
  name         String   @db.VarChar(191)
  email        String   @unique @db.VarChar(191)
  passwordHash String   @db.VarChar(255)
  chats             Chat[]
  memory            UserMemory?
  analyticsSessions AnalyticsSession[]
  futureSimulations FutureSimulation[]
  careerLabAttempts CareerLabAttempt[]
  impactParticipations ImpactParticipation[]
  impactReviewedContributions ImpactContribution[] @relation("ImpactContributionReviewer")
  impactPublicAuthorizations ImpactContribution[] @relation("ImpactContributionPublicAuthorizer")
  impactContributionEvents ImpactContributionEvent[] @relation("ImpactContributionEventActor")
  createdAt    DateTime @default(now()) @db.DateTime(3)
  updatedAt    DateTime @updatedAt @db.DateTime(3)

  @@map("app_users")
}

model CareerLabAttempt {
  id          String    @id @default(cuid()) @db.VarChar(191)
  userId      String    @db.VarChar(191)
  labKey      String    @db.VarChar(64)
  labVersion  Int
  areaKey     String    @db.VarChar(64)
  state       Json
  answers     Json
  skillScores Json
  reflection  String?   @db.VarChar(32)
  status      String    @db.VarChar(32)
  revision    Int       @default(0)
  startedAt   DateTime  @default(now()) @db.DateTime(3)
  completedAt DateTime? @db.DateTime(3)
  createdAt   DateTime  @default(now()) @db.DateTime(3)
  updatedAt   DateTime  @updatedAt @db.DateTime(3)
  user        User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, updatedAt])
  @@index([userId, labKey])
  @@index([id, userId, revision])
  @@map("app_career_lab_attempts")
}

model FutureSimulation {
  id                String   @id @default(cuid()) @db.VarChar(191)
  userId            String   @db.VarChar(191)
  selectedProgramId String   @db.VarChar(255)
  selectedOfferId   String   @db.VarChar(191)
  selectedCampusId  String   @db.VarChar(191)
  startingLevel     String   @db.VarChar(32)
  scenarioTrack     String   @db.VarChar(64)
  scenarioVersion   Int
  setup             Json
  state             Json
  choices           Json
  currentEventId    String?  @db.VarChar(96)
  stage             String   @db.VarChar(64)
  status            String   @db.VarChar(32)
  revision          Int      @default(0)
  user              User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt         DateTime @default(now()) @db.DateTime(3)
  updatedAt         DateTime @updatedAt @db.DateTime(3)

  @@index([userId, updatedAt])
  @@index([id, userId, revision])
  @@map("app_future_simulations")
}

model AnalyticsSession {
  id              String    @id @default(cuid()) @db.VarChar(191)
  userId          String    @db.VarChar(191)
  user            User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  startedAt       DateTime  @default(now()) @db.DateTime(3)
  lastSeenAt      DateTime  @default(now()) @db.DateTime(3)
  endedAt         DateTime? @db.DateTime(3)
  durationSeconds Int       @default(0)
  createdAt       DateTime  @default(now()) @db.DateTime(3)
  updatedAt       DateTime  @updatedAt @db.DateTime(3)

  @@index([userId, startedAt])
  @@index([lastSeenAt, endedAt])
  @@map("app_analytics_sessions")
}

model Chat {
  id                    String    @id @default(cuid()) @db.VarChar(191)
  title                 String    @db.VarChar(255)
  summary               String?   @db.VarChar(700)
  memoryEligibleTurnCount   Int    @default(0)
  memorySummarizedTurnCount Int    @default(0)
  educativeState        Json?
  educativeStateVersion Int       @default(0)
  userId                String    @db.VarChar(191)
  user                  User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  messages              Message[]
  createdAt             DateTime  @default(now()) @db.DateTime(3)
  updatedAt             DateTime  @updatedAt @db.DateTime(3)

  @@index([userId, updatedAt])
  @@map("app_chats")
}

model UserMemory {
  id        String   @id @default(cuid()) @db.VarChar(191)
  userId    String   @unique @db.VarChar(191)
  summary   String   @db.VarChar(1000)
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now()) @db.DateTime(3)
  updatedAt DateTime @updatedAt @db.DateTime(3)

  @@map("app_user_memories")
}

model Message {
  id        String      @id @default(cuid()) @db.VarChar(191)
  chatId    String      @db.VarChar(191)
  role      MessageRole
  content   String      @db.LongText
  uiAction  Json?
  chat      Chat        @relation(fields: [chatId], references: [id], onDelete: Cascade)
  createdAt DateTime    @default(now()) @db.DateTime(3)

  @@index([chatId, createdAt])
  @@map("app_messages")
}

enum MessageRole {
  user
  assistant
}

model ImpactZone {
  id           String            @id @default(cuid()) @db.VarChar(191)
  slug         String            @unique @db.VarChar(96)
  name         String            @db.VarChar(191)
  description  String            @db.Text
  targetPoints Int
  active       Boolean           @default(true)
  sortOrder    Int
  missions     ImpactMission[]
  events       ImpactZoneEvent[]
  createdAt    DateTime          @default(now()) @db.DateTime(3)
  updatedAt    DateTime          @updatedAt @db.DateTime(3)

  @@index([active, sortOrder])
  @@map("app_impact_zones")
}

model ImpactMission {
  id                 String                    @id @default(cuid()) @db.VarChar(191)
  zoneId             String                    @db.VarChar(191)
  slug               String                    @unique @db.VarChar(96)
  title              String                    @db.VarChar(191)
  summary            String                    @db.VarChar(500)
  problemDescription String                    @db.Text
  objective          String                    @db.Text
  instructions       String                    @db.Text
  deliverables       Json
  validationCriteria Json
  safetyNotes        String                    @db.Text
  difficulty         String                    @db.VarChar(32)
  estimatedMinutes   Int
  points             Int
  evidenceType       String                    @db.VarChar(64)
  participationMode  String                    @db.VarChar(32)
  publicationStatus  String                    @default("DRAFT") @db.VarChar(32)
  active             Boolean                   @default(true)
  startsAt           DateTime?                 @db.DateTime(3)
  endsAt             DateTime?                 @db.DateTime(3)
  sortOrder          Int
  zone               ImpactZone                @relation(fields: [zoneId], references: [id], onDelete: Cascade)
  capabilities       ImpactMissionCapability[]
  participations     ImpactParticipation[]
  createdAt          DateTime                  @default(now()) @db.DateTime(3)
  updatedAt          DateTime                  @updatedAt @db.DateTime(3)

  @@index([zoneId, publicationStatus, active])
  @@map("app_impact_missions")
}

model ImpactCapability {
  id             String                    @id @default(cuid()) @db.VarChar(191)
  slug           String                    @unique @db.VarChar(96)
  name           String                    @db.VarChar(191)
  description    String                    @db.Text
  category       String                    @db.VarChar(64)
  active         Boolean                   @default(true)
  missions       ImpactMissionCapability[]
  careerMappings ImpactCareerCapability[]
  createdAt      DateTime                  @default(now()) @db.DateTime(3)
  updatedAt      DateTime                  @updatedAt @db.DateTime(3)

  @@index([active, category])
  @@map("app_impact_capabilities")
}

model ImpactMissionCapability {
  missionId    String           @db.VarChar(191)
  capabilityId String           @db.VarChar(191)
  weight       Int
  mission      ImpactMission    @relation(fields: [missionId], references: [id], onDelete: Cascade)
  capability   ImpactCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  @@id([missionId, capabilityId])
  @@index([capabilityId])
  @@map("app_impact_mission_capabilities")
}

model ImpactParticipation {
  id           String              @id @default(cuid()) @db.VarChar(191)
  missionId    String              @db.VarChar(191)
  userId       String              @db.VarChar(191)
  status       String              @default("ACTIVE") @db.VarChar(32)
  revision     Int                 @default(0)
  startedAt    DateTime            @default(now()) @db.DateTime(3)
  completedAt  DateTime?           @db.DateTime(3)
  mission      ImpactMission       @relation(fields: [missionId], references: [id], onDelete: Cascade)
  user         User                @relation(fields: [userId], references: [id], onDelete: Cascade)
  contribution ImpactContribution?
  createdAt    DateTime            @default(now()) @db.DateTime(3)
  updatedAt    DateTime            @updatedAt @db.DateTime(3)

  @@unique([userId, missionId])
  @@index([userId, updatedAt])
  @@index([missionId, status])
  @@map("app_impact_participations")
}

model ImpactContribution {
  id                     String                    @id @default(cuid()) @db.VarChar(191)
  participationId        String                    @unique @db.VarChar(191)
  title                  String                    @db.VarChar(191)
  description            String                    @db.Text
  processNotes           String?                   @db.Text
  resultUrl              String?                   @db.VarChar(500)
  repositoryUrl          String?                   @db.VarChar(500)
  reflection             String?                   @db.Text
  status                 String                    @default("DRAFT") @db.VarChar(32)
  revision               Int                       @default(0)
  visibility             String                    @default("PRIVATE") @db.VarChar(32)
  publicAuthorizedAt     DateTime?                 @db.DateTime(3)
  publicAuthorizedById   String?                   @db.VarChar(191)
  reviewerId             String?                   @db.VarChar(191)
  reviewerEmailSnapshot  String?                   @db.VarChar(191)
  reviewerFeedback       String?                   @db.Text
  submittedAt            DateTime?                 @db.DateTime(3)
  reviewedAt             DateTime?                 @db.DateTime(3)
  approvedAt             DateTime?                 @db.DateTime(3)
  participation          ImpactParticipation       @relation(fields: [participationId], references: [id], onDelete: Cascade)
  publicAuthorizedBy     User?                     @relation("ImpactContributionPublicAuthorizer", fields: [publicAuthorizedById], references: [id], onDelete: SetNull)
  reviewer               User?                     @relation("ImpactContributionReviewer", fields: [reviewerId], references: [id], onDelete: SetNull)
  files                  ImpactContributionFile[]
  zoneEvents             ImpactZoneEvent[]
  auditEvents            ImpactContributionEvent[]
  createdAt              DateTime                  @default(now()) @db.DateTime(3)
  updatedAt              DateTime                  @updatedAt @db.DateTime(3)

  @@index([status, updatedAt])
  @@index([reviewerId, status])
  @@index([id, revision, status])
  @@map("app_impact_contributions")
}

model ImpactContributionFile {
  id             String             @id @default(cuid()) @db.VarChar(191)
  contributionId String             @db.VarChar(191)
  originalName   String             @db.VarChar(255)
  storageKey     String             @unique @db.VarChar(255)
  mimeType       String             @db.VarChar(127)
  fileSize       BigInt             @db.UnsignedBigInt
  visibility     String             @default("PRIVATE") @db.VarChar(32)
  contribution   ImpactContribution @relation(fields: [contributionId], references: [id], onDelete: Cascade)
  createdAt      DateTime           @default(now()) @db.DateTime(3)

  @@index([contributionId, createdAt])
  @@map("app_impact_contribution_files")
}

model ImpactCareerCapability {
  capabilityId       String           @db.VarChar(191)
  canonicalProgramKey String          @db.VarChar(191)
  weight             Int
  capability         ImpactCapability @relation(fields: [capabilityId], references: [id], onDelete: Cascade)

  @@id([capabilityId, canonicalProgramKey])
  @@index([canonicalProgramKey])
  @@map("app_impact_career_capabilities")
}

model ImpactZoneEvent {
  id             String             @id @default(cuid()) @db.VarChar(191)
  zoneId         String             @db.VarChar(191)
  contributionId String             @db.VarChar(191)
  eventType      String             @db.VarChar(64)
  points         Int
  description    String?            @db.VarChar(500)
  zone           ImpactZone         @relation(fields: [zoneId], references: [id], onDelete: Cascade)
  contribution   ImpactContribution @relation(fields: [contributionId], references: [id], onDelete: Cascade)
  createdAt      DateTime           @default(now()) @db.DateTime(3)

  @@unique([contributionId, eventType])
  @@index([zoneId, createdAt])
  @@map("app_impact_zone_events")
}

model ImpactContributionEvent {
  id                 String             @id @default(cuid()) @db.VarChar(191)
  contributionId     String             @db.VarChar(191)
  actorUserId        String?            @db.VarChar(191)
  actorEmailSnapshot String?            @db.VarChar(191)
  fromStatus         String?            @db.VarChar(32)
  toStatus           String             @db.VarChar(32)
  reason             String?            @db.Text
  contribution       ImpactContribution @relation(fields: [contributionId], references: [id], onDelete: Cascade)
  actor               User?              @relation("ImpactContributionEventActor", fields: [actorUserId], references: [id], onDelete: SetNull)
  createdAt           DateTime           @default(now()) @db.DateTime(3)

  @@index([contributionId, createdAt])
  @@index([actorUserId, createdAt])
  @@map("app_impact_contribution_events")
}
